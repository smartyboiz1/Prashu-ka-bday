Put your real files in this folder using these exact names
(or edit the paths in script.js under the CUSTOMIZATION section
if you'd rather use different names):

  class8.jpg               - your Class 8 handshake photo (Screen 3)
  friend-photo.jpg         - a small profile picture (Screen 3 avatar)
  birthday-voice.mp3       - your voice note / birthday message (Screen 3 audio player)
  background-music.mp3     - plays everywhere on loop from the first tap;
                             automatically ducks down while the Screen 3
                             voice note is playing, then comes back up
  cake-reveal-song.mp3     - a different song that takes over once he
                             blows out the candles on the final screen

  cake-photo-1.jpg  }
  cake-photo-2.jpg  } left side of the final message (top to bottom)
  cake-photo-3.jpg  }
  cake-photo-4.jpg  }
  cake-photo-5.jpg  } right side of the final message (top to bottom)
  cake-photo-6.jpg  }
    Exactly 6 photos that fade in, curving around the final birthday
    message, right after the candles are blown out.

You don't need to touch any HTML or CSS. Until a file exists here,
the website shows a cute placeholder in its place instead of breaking
(a missing cake photo simply doesn't appear, rather than leaving a gap).
